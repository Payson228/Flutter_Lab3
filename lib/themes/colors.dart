import 'package:flutter/material.dart';

class MyColors {
  // App colors
  static const Color appbarBackgroundColor = Color(0xFFBCDDA1);
  static const Color backgroundColor = Color(0xFFA9EB72);
  static const Color tabBarIndicator = Color(0xFF068441);
  static const Color primary = Color(0xFF08A652); //
  static const Color chipBackground = Color.fromRGBO(207, 219, 95, 0.44);

  // Text colors
  static const Color textTitleColor = Colors.black;
  static const Color textSubtitleColor = Color(0xFF08025E);
}
